# dependencies
node_modules/
.pnpm-store/

# builds
dist/
build/
.turbo/
.wrangler/
.astro/
.expo/
*.tsbuildinfo
# never ignore Claude skills (e.g. .claude/skills/build/ would match build/ above)
!.claude/skills/**

# env & secrets — never commit these
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*

# supabase local
supabase/.branches/
supabase/.temp/

# tooling
coverage/
playwright-report/
test-results/
.DS_Store
